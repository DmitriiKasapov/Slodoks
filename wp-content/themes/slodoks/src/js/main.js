/**
 * SloDoks theme scripts.
 */

import '../css/main.css';

/**
 * Temporary check that the Vite bundle is loaded and running.
 * Remove once real theme scripts land here.
 */
const helloWorld = () => {
	console.log('Hello world from the SloDoks theme');
};

document.addEventListener('DOMContentLoaded', () => {
	helloWorld();
});
