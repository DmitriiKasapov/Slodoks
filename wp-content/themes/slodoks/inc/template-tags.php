<?php
/**
 * Template helpers used across theme templates.
 *
 * The Bootstrap `nav-link` filter that used to live here was dropped together
 * with the vendor assets. Add helpers back as the Consal markup is integrated.
 *
 * @package SloDoks
 */

defined( 'ABSPATH' ) || exit;
